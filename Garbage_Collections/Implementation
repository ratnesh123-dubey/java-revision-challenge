public class GCDemo {
    public static void main(String[] args) {
        String s1 = new String("Hello");
        String s2 = new String("World");
        s1 = s2;  // "Hello" is now unreferenced → GC will collect it
        System.gc(); // Suggests JVM to run GC
    }
}
